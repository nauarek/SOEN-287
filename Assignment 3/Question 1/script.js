function addNumbers(arrayOfNumbers){
    var sum;
    for(var i; i<arrayOfNumbers.length; i++){
        sum += arrayOfNumbers[i];
    }
    return sum;
}

function getCurrentDate(){
    //get day of the week
    //get the month
    //get the day of the month
    //get the year

    //return as monday, may 10, 2010
}

function arrayToString(arrayOfWords){
    var longString;
    for(var i; i<arrayOfWords.length; i++){
        longString += arrayOfWords[i];
    }
    return arrayOfWords;
}

function findMaxNumber(){

}

function getDigits(string) {
    var digitsFromString;
    for(var i; i<string.length; i++){
        //scan string for digits
        //digitsFromString += //scanned digits;
    }
    return digitsFromString
}

function reverseString(string) {
    var rString;
    for(var i=string.length; i>=0 ; i--){
        rString += string[i];
    }
}

